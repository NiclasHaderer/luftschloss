{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "dryRun": {
      "type": "boolean",
      "description": "Do not actually publish the package to the registry."
    },
    "gitTagRelease": {
      "type": "boolean",
      "description": "Create a new git tag.",
      "default": true
    }
  },
  "required": []
}
